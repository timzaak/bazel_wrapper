#include "bazel_wrapper.h"
int main(int argc, char** argv) {

    hello_world();
  //std::cout << "Hello World" << std::endl;
  //google::InitGoogleLogging(argv[0]);
  //CHECK(mediapipe::PrintHelloWorld().ok());
  return 0;
}